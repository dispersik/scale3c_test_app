name: scale3c_test_app
description: Scale3c Test Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  core_ui:
    path: core_ui
  sign_in:
    path: feature/sign_in
  firebase_core: ^2.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  assets:
    - core_ui/assets/
  uses-material-design: true